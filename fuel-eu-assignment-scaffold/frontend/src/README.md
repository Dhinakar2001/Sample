# Frontend source folders (placeholders)

core/
  domain/  -> domain entities & types (no React)
  application/ -> use-cases, services
  ports/ -> inbound/outbound interface definitions
adapters/
  ui/ -> React components, pages (Routes, Compare, Banking, Pooling)
  infrastructure/ -> API clients calling backend endpoints
shared/ -> shared utilities, types

Create files per assignment requirements when implementing.
