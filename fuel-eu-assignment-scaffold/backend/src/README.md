# Backend source folders (placeholders)

core/ -> domain entities, use-cases, ports
adapters/ -> inbound HTTP + outbound Postgres adapters
infrastructure/ -> DB connection, server bootstrap
shared/ -> shared utils and types

Implement endpoints as specified in assignment when ready.
